{
  "name": "idea-pup",
  "private": true,
  "version": "0.1.0",
  "description": "Mochi — a lovable white puppy desktop companion for anytime brainstorming",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "desktop": "npm run build && electron .",
    "desktop:dev": "concurrently -k \"npm run dev -- --host 127.0.0.1\" \"wait-on http://127.0.0.1:5173 && ELECTRON_START_URL=http://127.0.0.1:5173 electron .\"",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^10.0.3",
    "electron": "^43.1.0",
    "vite": "^6.0.0",
    "wait-on": "^9.0.10"
  }
}
